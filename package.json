{
  "name": "draw-app",
  "private": true,
  "scripts": {
  "prebuild": "cd packages/db && npx prisma generate && cd ../../",
  "prebuild:deploy": "cd packages/db && npx prisma migrate deploy && npx prisma generate && cd ../../",
  "build": "npm run prebuild && turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
     "start:websocket": "cd apps/ws-backend && pnpm run dev",
    "build:websocket": "cd apps/ws-backend && pnpm run build && cd ../../",
    "build:backend": "cd apps/http-backend && pnpm run build && cd ../../",
      "start:backend": "cd apps/http-backend && pnpm run dev",
      "build:frontend":"cd apps/excelidraw-frontend && pnpm run build",
      "start:frontend": "cd apps/excelidraw-frontend && pnpm run start",
      "start:next":"pnpm run build:frontend && pnpm run start:frontend"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
